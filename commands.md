the touch command can be used to create a new file but you also
 need to include the name of the new file ex (touch filename)

cd is change direcoty
ls is list
clear clears the screen of all commmands but keeps you in the
file you were in

we can use "mv" to rename a file (mv commands commands.md

